import m from 'mithril';
import './index.scss';

export default (vnode) => {
  

  return {
    view(vnode) {
    

      return (
        <div class="graphType__card__wrapper">
          
        </div>
      );
    },
  };
};
